{
  "version": "1.0.7",
  "notes": "Fix one-click plugin installs picking incompatible jars: version matching now checks the full supported Minecraft range of each build (and prefers stable builds), so Essential Pack and marketplace installs always download the right jar for the configured server version."
}
